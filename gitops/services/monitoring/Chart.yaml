apiVersion: v2
name: kube-prometheus-stack
version: 1.0.0
description: A Helm chart for Prometheus stack
type: application
dependencies:
  - name: kube-prometheus-stack
    version: 86.0.1
    repository: https://prometheus-community.github.io/helm-charts
# annotations:
  # example: A list of annotations keyed by name (optional).